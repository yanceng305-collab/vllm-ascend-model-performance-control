# A3PerfRunner Prompt: GLM52-W8A8-PROFILE-CANDIDATE-FULL-MATRIX-VALIDATION

**For Task**: TASK-GLM52-W8A8-PROFILE-CANDIDATE-FULL-MATRIX-VALIDATION  
**Runner Role**: A3PerfRunner  
**Created**: 2026-09-03

---

## Role

You produce Evidence only. No Control commit/push (Release assets per D-022 are allowed), no Formal Result authoring, no Acceptance.

## Step 1 - Dispatch

Task ID: GLM52-W8A8-PROFILE-CANDIDATE-FULL-MATRIX-VALIDATION
DISPATCH_CONTROL_SHA: <from User>
Authorization: EXECUTE

Only proceed when all three are present.

## Step 2 - Warm service check (preferred path)

Verify the current reviewed warm service:

- PID 3164838; port 8000; log /workspace/glm52_od_64k_candidate.log
- /v1/models reachable; model glm52-w8a8; max_model_len 67000
- effective profile: gpu-memory-utilization=0.95, capture=96, MTP OFF, DP2/TP8/EP, seqs 48, batched 4096, async ON, multistream ON, FULL_DECODE_ONLY, prefix OFF

If EXACTLY matches: reuse (NO restart, NO re-graph-capture) for the whole matrix.
If service missing / identity mismatch / profile mismatch: restore the same frozen candidate (Task §2); parameter values must not change.

Record `warm-reuse.txt`: REUSE=YES/NO + verification output.

## Step 3 - Full matrix execution

For each cell in order 1K -> 4K -> 16K -> 64K (workload: input from [1024|4096|16384|65536], output 1024):

```bash
BASE_URL="http://127.0.0.1:8000"
MODEL_PATH="/data/tiankuan/zyg/model/GLM-5.2-w8a8"
SERVED_MODEL="glm52-w8a8"
RESULT_DIR="<TASK_EVIDENCE_DIR>/cell-<CELL>"
mkdir -p "$RESULT_DIR"
for RUN in 1 2 3 4; do
  vllm bench serve --backend vllm --base-url "$BASE_URL" --endpoint /v1/completions \
    --model "$SERVED_MODEL" --tokenizer "$MODEL_PATH" --trust-remote-code \
    --dataset-name random --random-input-len <INPUT> --random-output-len 1024 \
    --random-range-ratio 0 --request-rate inf --max-concurrency 64 --num-prompts 256 \
    --ignore-eos --save-result --result-dir "$RESULT_DIR" --result-filename "run${RUN}.json" \
    2>&1 | tee "$RESULT_DIR/run${RUN}.log"
done
```

Run1 = discard; Run2/3/4 = measured.

For each measured run: run extractor:

```bash
python scripts/extract_bench_metrics.py <cell-dir>/runN.log --out <cell-dir>/runN.metrics.json
```## Step 4 - Run quality gates

Each measured run (Run2/3/4):

- successful_requests == 256, failed_requests == 0
- runN.log kept raw; runN.json (if produced) kept; runN.metrics.json generated by the extractor

Client wedge (tqdm stuck) handling:

- mark that run attempt ABORTED / INVALID; do not count it; re-run the SAME run with the same profile.
- (this is not a parameter change.)

Engine crash / device error / OOM / KV rejection / correctness failure / profile mismatch: record Evidence; do NOT change parameters; STOP the cell and report FAIL per formal rules.

## Step 5 - Machine verification (no hand numbers)

Run per cell:

```bash
python scripts/validate_matrix_candidate.py --cell-dir <cell_dir> --cell <CELL>
```

Exit 0 = PASS, 1 = FAIL. Keep its JSON report in Evidence.

## Step 6 - Evidence (all outcomes)

Per cell dir: launch/log/profile photo, run1-4 logs, metrics JSONs, README-record, warm-reuse.txt, control-sha.txt, runtime-identity, environment, per-cell validation JSON, aggregations (mean run2-4, delta vs baseline, achievement, 80% target, stability table min/max/mean/stddev/CV%), SHA256SUMS/MANIFEST, single tar.gz per D-022.

## Step 7 - Runner report

Report per cell: mean, delta %, achievement %, stability (std/CV), 256/0 for Run23-4, summary; profile consistency across cells; final POI (skip optional). No Formal Result authored.

Keep warm service running after the matrix (SERVICE_LEFT_RUNNING=YES + PID/port/log), unless failure requires a stop of Task-owned processes only.

## Constraints

- Frozen profile; no per-cell tuning.
- Do not modify vLLM/site-packages/image to fix the JSON writer.
- 64K cell = same contract; D-024; evidence on every outcome; D-022 upload.
- Formal OPT-01 untouched.